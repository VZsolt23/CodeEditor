<UserControl x:Class="CodeEditor.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit">

    <avalonedit:TextEditor x:Name="Editor"
                           Document="{Binding Document}"
                           IsReadOnly="{Binding IsReadOnly}"
                           SyntaxHighlighting="{Binding SyntaxHighlighting}"
                           FontFamily="{Binding Options.FontFamily}"
                           FontSize="{Binding Options.FontSize}"
                           WordWrap="{Binding Options.WordWrap}"
                           ShowLineNumbers="{Binding Options.ShowLineNumbers}"
                           Background="{DynamicResource Brush.Editor.Background}"
                           Foreground="{DynamicResource Brush.Editor.Foreground}"
                           LineNumbersForeground="{DynamicResource Brush.Editor.LineNumber}"
                           HorizontalScrollBarVisibility="Auto"
                           VerticalScrollBarVisibility="Auto"
                           Padding="10,6,0,0" />

</UserControl>
